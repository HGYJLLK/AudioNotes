# Web Framework
chainlit>=1.3.0

# Logging
loguru==0.7.3

# ASR (Automatic Speech Recognition)
funasr
modelscope
torch==2.2.2
torchaudio

# Alternative ASR (optional, can be removed if not used)
# faster-whisper

# LLM Integration
openai==1.63.2
langchain

# Database (only needed if using PostgreSQL data layer)
# asyncpg
# sqlalchemy[asyncio]
# sqlalchemy_utils
# psycopg2-binary

# Core SQLAlchemy (already installed)
sqlalchemy==2.0.41

# Environment Management
python-dotenv==1.0.1
